spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: '1234'
      username: user
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  instance:      
    metadata-map:
      management.context-path: /uaa/management
    status-page-url-path: /uaa/management/info
    health-check-url-path: /uaa/management/health